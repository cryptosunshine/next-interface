import * as localeTypes from './types';

export const en: localeTypes.LocaleData = {
    share: {
        more: 'more',
    },
    '/404': {
        pageNotExists: 'page not exists',
        backHome: 'back home'
    }
}
